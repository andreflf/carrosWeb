export class LoginM {
  //tem que ser os mesmos nomes usados no login do backend
  //porque os dados serao enviados no body da requisiçao do logar() da classe LoginService
    username!: string;
    password!:string;

    //constructor(usuario: string, senha: string){
   //     this.senha=senha;
 //       this.usuario=usuario;
   // }
}
